registry=https://registry.npmjs.org/
fetch-timeout=600000
fetch-retries=5
network-concurrency=3
# Flat node_modules instead of pnpm's nested .pnpm store -- avoids Windows'
# 260-char path limit (deep .pnpm folder names + nested paths blow past it).
node-linker=hoisted
